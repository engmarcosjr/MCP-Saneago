# Contrato HTTP — LRS010 (Distribuição de Serviço)

- **URL ZK:** `/prt/lrs/LRS010DistribuicaoServico.zul`
- **Status:** ARVORE CAPTURADA (sem POST)
- **Natureza:** ESCRITA (aqui so leitura — nunca operar botao de gravacao)
- **Origem:** `scratch/arvores/LRS010.html` (GET da arvore, 305 widgets)
- **Titulo na tela:** LRS010 - Distribuição de Serviços - Água, Esgoto e Comercial   /   Nível de Acesso: CAIE

> **Atencao — leia antes de automatizar.** Os ids, tipos e colunas abaixo sao
> reais, extraidos da arvore de widgets da tela. Mas a **sequencia de POSTs**
> desta tela ainda **nao foi observada**: use as regras gerais de
> `docs/HTTP-ECO707-ECO709.md` como hipotese e confirme contra a tela antes de
> confiar. Um POST em campo errado faz o ZK responder vazio **sem erro algum**.

> **Tela de escrita.** Este documento cobre apenas a abertura e a leitura da
> estrutura. Nenhum botao de gravacao foi acionado, e nenhum deve ser acionado
> sem o gate de escrita do MCP.

## Abertura

```http
GET /prt/lrs/LRS010DistribuicaoServico.zul
Host: prod.saneago.com.br
Referer: https://prod.saneago.com.br/prt/mpt/principal.zul
```

A resposta traz o `dtid` e a arvore serializada. Os `uuid` mudam a cada abertura:
resolva sempre pelo **id estavel** com `uuidByComponentId` de `src/http/zk-tree.js`.

## Campos (ids estaveis reais)

| id estavel | rotulo na tela | classe ZK | tipo do `data_N` | obs |
|---|---|---|---|---|
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `dtbxPeriodoInicial` | Período: | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | — |
| `dtbxPeriodoFinal` | a | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | — |
| `cmbbxSituacao` | Situação: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | — |
| `intbxCodigoDS` | Código da DS: | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | — |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtbxCodigoEquipe` | : | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtbxResponsavelPelaEquipe` | : | `zul.inp.Textbox` | string — `{"value":"texto"}` | somente leitura |
| `txtbxPlacaVeiculo` | Placa da viatura: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `dtbxData` | Data: | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | — |
| `intbxProgs` | / | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | — |

## Botoes

| id estavel | rotulo | natureza |
|---|---|---|
| `btnConsultarPesquisarRA` | Consultar | leitura |
| `btnIncluirPesquisarRA` | Incluir na DS   | **ESCRITA — exige gate** |
| `btnLimparPesquisarRA` | Limpar Pesquisa | leitura |
| `btnConfirmarImpressao` | Emitir | **ESCRITA — exige gate** |
| `btnCancelarImpressao` | Cancelar | leitura |
| `btnListarEquipes` | — | leitura |
| `btnListarTodasPlacas` | — | leitura |
| `btnAdicionar` | Adicionar | leitura |
| `btnRemover` | Remover | leitura |
| `btnPesquisarRA` | Pesquisar RAs | leitura |
| `btnConsultar` | Consultar | leitura |
| `btnIncluir` | Incluir | **ESCRITA — exige gate** |
| `btnAlterar` | Alterar | **ESCRITA — exige gate** |
| `btnImprimir` | Emitir | leitura |
| `btnCancelar` | Cancelar | leitura |

## Grade de resultado

Listbox: `lstPesquisa`, `lstPesquisa`, `lstbxListaPesquisarRA`, `lstPesquisa`, `lstbxListaResponsavavelPorEquipe`, `lstbxViatura`, `lstbxListaRA`

1. Código
2. Nome
3. Código
4. Nome
5. Prioritário
6. RA/Prog
7. Endereço
8. Serviço
9. Data vencimento
10. Situação vencimento
11. Código
12. Nome
13. Equipe
14. Nome Responsável
15. Tipo Equipe
16. Categoria
17. Código
18. Placa
19. Marca
20. Modelo
21. Prioritário
22. RA/Prog
23. Endereço
24. Serviço

Ancore a leitura pelo conteudo (ex.: RA por `/^\d{9,11}$/`), nao contando celulas:
colunas de botao entram como celulas vazias.

## Protocolo

As regras gerais do ZK por HTTP (onChange+onClick no mesmo POST, `echo` com
`opt=i`, reuso do `dtid`, queda de sessao) estao em `docs/HTTP-ECO707-ECO709.md`.
Elas valem para qualquer tela — mas o payload concreto so esta confirmado nas telas
com status `REPLICADO`.

---
*Gerado por `src/gerar_doc_http.js` em 2026-08-06. Nao editar a mao: reexecute o gerador.*
