# Contrato HTTP — LRS105 (Lançamento de serviços executados)

- **URL ZK:** `/prt/lrs/LRS105CadastraRetornoRA.zul`
- **Status:** ARVORE CAPTURADA (sem POST)
- **Natureza:** ESCRITA (aqui so leitura — nunca operar botao de gravacao)
- **Origem:** `scratch/arvores/LRS105.html` (GET da arvore, 1026 widgets)
- **Titulo na tela:** LRS105 - Lançamento de serviços executados   /   Nível de Acesso: CAIE

> **Atencao — leia antes de automatizar.** Os ids, tipos e colunas abaixo sao
> reais, extraidos da arvore de widgets da tela. Mas a **sequencia de POSTs**
> desta tela ainda **nao foi observada**: use as regras gerais de
> `docs/HTTP-ECO707-ECO709.md` como hipotese e confirme contra a tela antes de
> confiar. Um POST em campo errado faz o ZK responder vazio **sem erro algum**.

> **Tela de escrita.** Este documento cobre apenas a abertura e a leitura da
> estrutura. Nenhum botao de gravacao foi acionado, e nenhum deve ser acionado
> sem o gate de escrita do MCP.

## Abertura

```http
GET /prt/lrs/LRS105CadastraRetornoRA.zul
Host: prod.saneago.com.br
Referer: https://prod.saneago.com.br/prt/mpt/principal.zul
```

A resposta traz o `dtid` e a arvore serializada. Os `uuid` mudam a cada abertura:
resolva sempre pelo **id estavel** com `uuidByComponentId` de `src/http/zk-tree.js`.

## Campos (ids estaveis reais)

| id estavel | rotulo na tela | classe ZK | tipo do `data_N` | obs |
|---|---|---|---|---|
| `intBxNumeroContaHistorico` | : | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | somente leitura |
| `intBxLigacaoSelecionada` | : | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | somente leitura |
| `intbxProgramacao` | Programação: | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | — |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `intbxCodigoServicoSolicitado` | Serviço Solicitado: | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | somente leitura |
| `txtbxDescricaoServicoSolicitado` | Serviço Solicitado: | `zul.inp.Textbox` | string — `{"value":"texto"}` | somente leitura |
| `txtCodigo` | Distrito: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtbxSituacao` | Situação: | `zul.inp.Textbox` | string — `{"value":"texto"}` | somente leitura |
| `dtbxDataDistribuicao` | Data distribuição: | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | — |
| `dtbxDataHoraDigitacao` | Data/Hora Digitação: | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | somente leitura |
| `txtbxDigitador` | Digitador: | `zul.inp.Textbox` | string — `{"value":"texto"}` | somente leitura |
| `txtbxCodigoEquipe` | Equipe: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtbxResponsavelPelaEquipe` | Equipe: | `zul.inp.Textbox` | string — `{"value":"texto"}` | somente leitura |
| `intbxNumeroFrota` | Placa do Veículo | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | invisivel |
| `txtbxPlacaViatura` | Placa do Veículo | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `intbxCodigoViatura` | Placa do Veículo | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | invisivel |
| `dtbxHoraComunicacao` | Hora da comunicação: | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | — |
| `dtbxHoraInicioExecucao` | Hora início execução: | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | — |
| `dtbxHoraFinalExecucao` | Hora final execução: | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | — |
| `intbxKmInicio` | Km início deslocamento: | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | — |
| `intbxKmFinal` | Km final deslocamento: | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | — |
| `dtbxDataExecucao` | Data execução serviço: | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | somente leitura |
| `cmbbxHoraExtra` | Serviço e hora extra (S/N): | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbdmnServicoExecutado` | Serviço executado por maquina: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbdmnLocalVazamentoPadrao` | Local vazamento padrão: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `intbxQuantidadeServicos` | Quantidade serviços executados: | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | — |
| `txtbxObservacao` | Observação: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `dcmbxQuantidadeMaterial` | Quantidade: | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | — |
| `cmbdmnLocalVazamentoAgua` | Local do vazamento: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbbxTipoVazamentoAgua` | Tipo do vazamento: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | — |
| `cmbdmnVazamentoDerivacaoAgua` | Derivação | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbdmnVazamentoRedeAgua` | Rede | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbdmnVazamentoRamalAgua` | Ramal: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbdmnVazamentoCavaleteAgua` | Cavalete: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbdmnCausaVazamentoAgua` | Causa do vazamento: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbbxDesobstrucaoConserto` | Desobstrução ou Conserto: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbdmnLocalDesobstrucaoEsgoto` | Local da Desobstrução: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbdmnLocalConsertoEsgoto` | Local do Conserto: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbdmnCausaEsgoto` | Causa: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `chkRua` | Selecione para Excluir: | `zul.wgt.Checkbox` | booleano — `{"checked":true}` | — |
| `cmbdmnTipoRua` | Tipo: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `dcmlbxLarguraCorteRua` | Dimensão corte (L X C): | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | — |
| `dcmlbxComprimentoCorteRua` | m | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | — |
| `dcmlbxAreaCorteRua` | = | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | somente leitura |
| `dtbxDataHoraValidacao` | Data/Hora Validação: | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | somente leitura |
| `txtbxResponsavelValidacao` | Responsável Validação: | `zul.inp.Textbox` | string — `{"value":"texto"}` | somente leitura |
| `intbxNumeroLoteAsfalto` | Nº Lote: | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | somente leitura |
| `dtbxDataHoraEnvioEMail` | Data/Hora envio E-mail: | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | somente leitura |
| `txtbxEMail` | E-Mail: | `zul.inp.Textbox` | string — `{"value":"texto"}` | somente leitura |
| `chkPasseio` | Selecione para Excluir: | `zul.wgt.Checkbox` | booleano — `{"checked":true}` | — |
| `cmbdmnTipoPasseio` | Tipo: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `dcmlbxLarguraCortePasseio` | Dimensão corte (L X C): | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | — |
| `dcmlbxComprimentoCortePasseio` | m | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | — |
| `dcmlbxAreaCortePasseio` | = | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | somente leitura |
| `dcmlbxProfundidadeCortePasseio` | Espessura: | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | — |
| `chkMureta` | Selecione para Excluir: | `zul.wgt.Checkbox` | booleano — `{"checked":true}` | — |
| `cmbdmnTipoMureta` | Tipo: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `dcmlbxLarguraCorteMureta` | Dimensão corte (L X C): | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | — |
| `dcmlbxComprimentoCorteMureta` | m | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | — |
| `dcmlbxAreaCorteMureta` | = | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | somente leitura |
| `dcmlbxProfundidadeCorteMureta` | Espessura: | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | — |
| `cmbdmnResponsavelDano` | Responsável pelo dano: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbbxServicoNaoConformidade` | Serviço: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | — |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | somente leitura |
| `dtbxDataCancelamento` | * | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | somente leitura |
| `comboDominioServicoPrestado` | * | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `txtboxDescricaoCancelamento` | Descrição: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `intBxCodigoLigacaoConta` | * | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | somente leitura, invisivel |
| `dtBxDataVisita` | * | `zul.db.Datebox` | data — `{"value":"2024.1.1.0.0.0.0","z$dateKeys":["value"]}` (mes 1-based) | somente leitura, invisivel |
| `intBxNumeroConta` | * | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | somente leitura |
| `cmbxLigacaoHidrometro` | * | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `intBxNumeroLacre` | Número do Lacre: | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | — |
| `cmbDmnTpLacre` | * | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cmbDmnTpEntrega` | * | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |

## Botoes

| id estavel | rotulo | natureza |
|---|---|---|
| — | Voltar | leitura |
| `btnConsultar` | Consultar | leitura |
| `btnGoCancelamento` | RA Atendida Sem Exc. | leitura |
| `btnCancelar` | Cancelar | leitura |
| `btnListarTodasEquipes` | Listar Todas | leitura |
| `btnListarTodasPlacas` | Listar Todas | leitura |
| `btnIncluirExecucao` | Incluir | **ESCRITA — exige gate** |
| `btnAlterarExecucao` | Alterar | **ESCRITA — exige gate** |
| `btnExcluirExecucao` | Excluir | **ESCRITA — exige gate** |
| `btnCancelarExecucao` | Cancelar | leitura |
| `btnAdicionarMaterial` | Adicionar | leitura |
| `btnAlterarMaterial` | Gravar | **ESCRITA — exige gate** |
| `btnCancelarMaterial` | Cancelar | leitura |
| `btnIncluirEstatistica` | Incluir | **ESCRITA — exige gate** |
| `btnAlterarEstatistica` | Alterar | **ESCRITA — exige gate** |
| `btnExcluirEstatistica` | Excluir | **ESCRITA — exige gate** |
| `btnCancelarEstatistica` | Cancelar | leitura |
| `btnIncluirCancelamento` | Incluir | **ESCRITA — exige gate** |
| `btnAlterarCancelamento` | Alterar | **ESCRITA — exige gate** |
| `btnCancelarCancelamento` | Cancelar | leitura |
| `btnVoltarCancelamento` | Voltar | leitura |
| `btnIncluirLacre` | Incluir | **ESCRITA — exige gate** |
| `btnAlterarLacre` | Alterar | **ESCRITA — exige gate** |
| `btnCancelarLacre` | Cancelar | leitura |
| `btnVoltarLacre` | Voltar | leitura |

## Grade de resultado

Listbox: `lstPesquisa`, `lstPesquisa`, `lstbxRA`, `lstbxServicoResposta`, `lstbxServico`, `lstbxServicoAtendido`, `lstbxEquipe`, `lstbxViatura`, `lstPesquisa`, `lstbxHistoricoMaterialBaixado`, `lstbxMaterial`, `lstbxMaterialBaixado`, `lstPesquisa`, `lstbxCancelamento`, `lstBxLaceacaoHidrometro`, `lstbxEstoqueDetalhado`

1. Hidrômetro
2. Data Lacração
3. Tipo de Lacre
4. Número Lacre
5. Forma de Entrega
6. Código
7. Descrição
8. Codigo
9. Nome
10. Sigla
11. Código
12. Cidade
13. RA
14. Programação
15. Data Solicitação
16. Serviço Solicitação
17. Serviço Resposta
18. Código
19. Descrição
20. Código
21. Descrição
22. Código
23. Descrição
24. Equipe
25. Nome Responsável
26. Tipo Equipe
27. Categoria
28. Frota
29. Placa
30. Marca
31. Modelo
32. Cor
33. Código material no SAP
34. Descrição Sucinta 
35. Quantidade
36. Unidade de Medida
37. Código SAP
38. Descrição
39. Nº doc. (baixa SAP)
40. Quantidade
41. Unidade de Medida
42. Código SAP
43. Descrição
44. Qtde Estoque Viatura
45. Excluir Material
46. Quantidade
47. Unidade de Medida
48. Código SAP
49. Descrição
50. Nº doc. (baixa SAP)
51. Número da matrícula do operador
52. Nome do Operador
53. RA
54. Programação
55. Responsável pelo Cadastro
56. Data do Cancelamento
57. Motivo do Cancelamento
58. Observação
59. Situação
60. Conta
61. Hidrômetro
62. Número Lacre
63. Data Lacração
64. Tipo de Lacre
65. Forma de Entrega
66. Estoque
67. Saldo

Ancore a leitura pelo conteudo (ex.: RA por `/^\d{9,11}$/`), nao contando celulas:
colunas de botao entram como celulas vazias.

## Protocolo

As regras gerais do ZK por HTTP (onChange+onClick no mesmo POST, `echo` com
`opt=i`, reuso do `dtid`, queda de sessao) estao em `docs/HTTP-ECO707-ECO709.md`.
Elas valem para qualquer tela — mas o payload concreto so esta confirmado nas telas
com status `REPLICADO`.

---
*Gerado por `src/gerar_doc_http.js` em 2026-08-07. Nao editar a mao: reexecute o gerador.*
