# Contrato HTTP — ECO154 (Usuários por Nome)

- **URL ZK:** `/prt/eco/ECO154ConsultaUsuario.zul`
- **Status:** REPLICADO
- **Natureza:** consulta
- **Origem:** `scratch/arvores/ECO154.html` (GET da arvore, 234 widgets)
- **Titulo na tela:** ECO154 - Consulta Usuários   /   Nível de Acesso: C***

## Abertura

```http
GET /prt/eco/ECO154ConsultaUsuario.zul
Host: prod.saneago.com.br
Referer: https://prod.saneago.com.br/prt/mpt/principal.zul
```

A resposta traz o `dtid` e a arvore serializada. Os `uuid` mudam a cada abertura:
resolva sempre pelo **id estavel** com `uuidByComponentId` de `src/http/zk-tree.js`.

## Campos (ids estaveis reais)

| id estavel | rotulo na tela | classe ZK | tipo do `data_N` | obs |
|---|---|---|---|---|
| `txtCodigo` | Logradouro: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtCodigo` | Logradouro: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtbxQuadraLogradouro` | Quadra: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtbxLoteLogradouro` | Lote: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtbxNumeroLogradouro` | Número: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtbxNomeProprietario` | Nome: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `cboTipoBusca` | Tipo de Busca: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cboEscopoBusca` | Escopo de busca: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `cboOrdemBusca` | Ordem de busca: | `zul.inp.Combobox` | combo — `onChange` com o texto + `onSelect` do item | somente leitura |
| `txtbxCpfCnpj` | CPF/CNPJ: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |

## Botoes

| id estavel | rotulo | natureza |
|---|---|---|
| `btnConsultar` | Consultar | leitura |
| `btnImprimir` | Imprimir | leitura |
| `btnCancelar` | Cancelar | leitura |

## Grade de resultado

Listbox: `lstPesquisa`, `lstPesquisa`, `lstPesquisa`, `lstPesquisa`, `lstbxUsuario`

1. Codigo
2. Nome
3. Código
4. Cidade
5. Hierarquia
6. Código
7. Nome
8. Código
9. Nome
10. Codigo
11. Nome
12. Codigo
13. Nome
14. ECO151
15. Nº Conta
16. Nome Proprietário
17. Logradouro
18. S.A.
19. S.E.
20. Quadra
21. Lote
22. Nº
23. Codificação.
24. Id. Conta.
25. Hidrômetro
26. ECO157
27. ECO707

Ancore a leitura pelo conteudo (ex.: RA por `/^\d{9,11}$/`), nao contando celulas:
colunas de botao entram como celulas vazias.

## Protocolo

As regras gerais do ZK por HTTP (onChange+onClick no mesmo POST, `echo` com
`opt=i`, reuso do `dtid`, queda de sessao) estao em `docs/HTTP-ECO707-ECO709.md`.
Elas valem para qualquer tela — mas o payload concreto so esta confirmado nas telas
com status `REPLICADO`.

---
*Gerado por `src/gerar_doc_http.js` em 2026-08-06. Nao editar a mao: reexecute o gerador.*
