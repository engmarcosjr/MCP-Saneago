# Contrato HTTP — LRS100 (Manter estoque de Material por viatura)

- **URL ZK:** `/prt/lrs/LRS100ManterEstoqueMaterialPorViatura.zul`
- **Status:** ARVORE CAPTURADA (sem POST)
- **Natureza:** consulta
- **Origem:** `scratch/arvores/LRS100.html` (GET da arvore, 316 widgets)
- **Titulo na tela:** LRS100 - Controle de Estoque de Materiais por Viaturas   /   Nível de Acesso: CAIE

> **Atencao — leia antes de automatizar.** Os ids, tipos e colunas abaixo sao
> reais, extraidos da arvore de widgets da tela. Mas a **sequencia de POSTs**
> desta tela ainda **nao foi observada**: use as regras gerais de
> `docs/HTTP-ECO707-ECO709.md` como hipotese e confirme contra a tela antes de
> confiar. Um POST em campo errado faz o ZK responder vazio **sem erro algum**.

## Abertura

```http
GET /prt/lrs/LRS100ManterEstoqueMaterialPorViatura.zul
Host: prod.saneago.com.br
Referer: https://prod.saneago.com.br/prt/mpt/principal.zul
```

A resposta traz o `dtid` e a arvore serializada. Os `uuid` mudam a cada abertura:
resolva sempre pelo **id estavel** com `uuidByComponentId` de `src/http/zk-tree.js`.

## Campos (ids estaveis reais)

| id estavel | rotulo na tela | classe ZK | tipo do `data_N` | obs |
|---|---|---|---|---|
| `txtCodigo` | Distrito: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtbxPlacaViatura` | Veículo | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `intbxViatura` | Veículo | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | invisivel |
| `intbxCodigoViatura` | Veículo | `zul.inp.Intbox` | inteiro — `{"value":12345}` (mandar string faz consultar vazio em silencio) | invisivel |
| `txtDescricao` | Informe o conteúdo para pesquisa e tecle (Enter) | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtbxInicioPeriodo` | Período: | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `txtbxFimPeriodo` | a | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |
| `dcmlbxQuantidade` | Quantidade | `zul.inp.Decimalbox` | decimal — `{"value":12.34}` | — |
| `txtbxObservacao` | Observacao | `zul.inp.Textbox` | string — `{"value":"texto"}` | — |

## Botoes

| id estavel | rotulo | natureza |
|---|---|---|
| `btnConfirmarImpressaoMateriasViatura` | Confirmar | **ESCRITA — exige gate** |
| `btnCancelarImpressaoMateriasViatura` | Cancelar | leitura |
| `btnListarViaturas` | Listar Todas | leitura |
| `btnConsultar` | Consultar | leitura |
| `btnIncluirSistema` | Incluir Sem SAP | **ESCRITA — exige gate** |
| `btnDevolverSistema` | Devolver Sem SAP | leitura |
| `btnIncluir` | Incluir | **ESCRITA — exige gate** |
| `btnDevolver` | Devolver | leitura |
| `btnDevolverTudo` | Devolver Tudo | leitura |
| `btnImprimir` | Imprimir | leitura |
| `btnCancelar` | Cancelar | leitura |

## Grade de resultado

Listbox: `lstPesquisa`, `lstPesquisa`, `lstbxViatura`, `lstPesquisa`, `lstbxMateriais`, `lstbxEstoqueDetalhado`

1. Codigo
2. Nome
3. Sigla
4. Código
5. Cidade
6. Código
7. Descrição
8. Frota
9. Placa
10. Marca
11. Modelo
12. Cor
13. Código material no SAP
14. Descrição Sucinta 
15. Código
16. Código SAP
17. Descrição
18. Qtdade Estoque Veículo
19. Utilizado Sem baixa no SAP
20. Unidade Medida
21. RA\'s Pendentes
22. Estoque
23. Saldo

Ancore a leitura pelo conteudo (ex.: RA por `/^\d{9,11}$/`), nao contando celulas:
colunas de botao entram como celulas vazias.

## Protocolo

As regras gerais do ZK por HTTP (onChange+onClick no mesmo POST, `echo` com
`opt=i`, reuso do `dtid`, queda de sessao) estao em `docs/HTTP-ECO707-ECO709.md`.
Elas valem para qualquer tela — mas o payload concreto so esta confirmado nas telas
com status `REPLICADO`.

---
*Gerado por `src/gerar_doc_http.js` em 2026-08-07. Nao editar a mao: reexecute o gerador.*
