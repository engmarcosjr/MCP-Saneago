# Contrato HTTP — ECO205 (Consulta Hidrômetro)

URL ZK: `/prt/eco/ECO205ConsultaHidrometro.zul`  
Natureza: **CONSULTA**  
Status: **CONFIRMADO**

## Descrição Operacional
Consulta de características técnicas e histórico de alocação de hidrômetro por número de série.

## 1. Requisição Inicial (GET)
```http
GET /prt/eco/ECO205ConsultaHidrometro.zul HTTP/1.1
Host: prod.saneago.com.br
Referer: https://prod.saneago.com.br/prt/mpt/principal.zul
```

A resposta HTML/ZK inicial fornece o desktop id (`dtid`) e a árvore de componentes serializada.

## 2. Componentes e Campos de Filtro (IDs Estáveis)

| ID Estáve | Rótulo Na Tela | Tipo de Dado ZK | Somente Leitura |
|---|---|---|---|
| `txtbxNumeroHidrometro` | Número Hidrômetro | `Textbox` | Não |
| `intbxConta` | Número Conta | `Intbox` | Não |

> **Nota de Tipo**:
> - `Intbox` (`zul.inp.Intbox`): Enviar número inteiro (`{"value": 12345}`).
> - `Datebox` (`zul.db.Datebox`): Formato `ano.mês.dia.hora.min.seg.ms` (`1-based`, ex: `2024.1.1.12.0.0.0`).
> - `Macro Div`: Usar o utilitário `resolverFilho(idMacro, 'txtCodigo')` de `src/http/zk-tree.js` para obter o UUID exato do campo interno.

## 3. Botões e Ações

| ID Estáve | Rótulo No Botão | Ação / Natureza |
|---|---|---|
| `btnConsultar` | Consultar | Consulta |
| `btnLimpar` | Limpar | Limpar |

## 4. Estrutura de Retorno (Colunas da Grade)
- `Número Série`
- `Marca/Modelo`
- `Vazão Nominal`
- `Conta Atual`
- `Data Instalação`
- `Situação`

## 5. Exemplo de Payload POST (/prt/zkau)
```http
POST /prt/zkau HTTP/1.1
Host: prod.saneago.com.br
Content-Type: application/x-www-form-urlencoded;charset=UTF-8

dtid=<dtid>
&cmd_0=onChange &uuid_0=<uuid_campo> &data_0={"value": ...}
&cmd_1=onClick  &uuid_1=<uuid_btn_consultar> &data_1={"pageX":0,"pageY":0,"which":1,"x":0,"y":0}

POST /prt/zkau (Echo de retorno)
dtid=<dtid>
&cmd_0=echo &opt_0=i &uuid_0=<uuid_echo> &data_0={"":["onConsultar"]}
```

## 6. Status Final
Status: **CONFIRMADO**  
Origem dos Dados: Varredura oficial ZK / `config/capacidades.json` + capturas em `scratch/zkau_ECO205.txt`.
