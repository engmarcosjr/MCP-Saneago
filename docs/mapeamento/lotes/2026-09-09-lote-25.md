# Diário de Bordo — Lote 25

- **Data:** 2026-09-09
- **Executor:** omniclaude
- **Aplicações do Lote:** ECO349, ECO351, ECO357

---

## 1. O que foi feito

1. **Inspeção de Telas ZK:**
   - `ECO349` — Concessão de Crédito: tela para consultar concessão de crédito de óleo fornecido por unidade de atendimento, tipo de gerador, conta ou CPF/CNPJ. 10 campos de entrada (textos e rádios), 2 botões acionáveis (`Consultar`, `Cancelar`) e 2 botões técnicos auxiliares (`-btn`). Classe proposta: `somente_leitura`.
   - `ECO351` — Listagem Coleta Óleo: tela para consultar e listar coletas de óleo por unidade de atendimento, tipo de gerador, situação e período. 11 campos de entrada (textos, rádios e datas), 2 botões acionáveis (`Consultar`, `Cancelar`) e 3 botões técnicos auxiliares (`-btn`). Classe proposta: `somente_leitura`.
   - `ECO357` — Consulta Estoque: tela para consultar o estoque de materiais por Unidade Operacional de entrega. 2 campos de entrada (textos), 2 botões acionáveis (`Consultar`, `Cancelar`) e 1 botão técnico auxiliar (`-btn`). Classe proposta: `somente_leitura`.

2. **Evidências e Fichas:**
   - Evidências brutas salvas em `docs/mapeamento/evidencias/{ECO349,ECO351,ECO357}.inspect.json`.
   - Fichas técnicas criadas em `docs/apps/{ECO349,ECO351,ECO357}.md`.
   - Catálogo de runtime atualizado em `config/roteiro.json`.
   - Safelist atualizada em `docs/SAFELIST_LEITURA.md`.

3. **Auditoria e Hashes:**
   - Entradas registradas no `docs/mapeamento/AUDITORIA.jsonl` com cálculo estrito de SHA-256 das evidências e fichas e segregação de botões técnicos auxiliares em `botoes_ignorados`.

---

## 2. Prova de Execução

```bash
$ node scripts/auditar-mapeamento.js && npm test
Auditadas 72 apps · 69 com entrada em roteiro.json
0 divergências.

> mcp-saneago@1.0.0 test
> node --test test/*.test.js

# tests 89
# suites 0
# pass 89
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

---

## 3. Decisões e Dúvidas para o Gate Humano

- **ECO349, ECO351, ECO357:** Todas propostas como `somente_leitura`, pois contêm exclusivamente botões de consulta e cancelamento sem persistência ou alteração de dados.
