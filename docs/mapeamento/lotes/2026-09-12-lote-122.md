# Diário de Bordo — Mapeamento Lote 122

- **Data:** 2026-09-12
- **Lote:** lote-122
- **Executor:** omniclaude
- **Aplicações do Lote:** EGWV005, EGWV402, FGCV018

---

## O que foi tentado

Inspeção direta e inventário do lote 122:
1. Execução direta da captura automatizada via Playwright para `EGWV005`, `EGWV402` e `FGCV018`.
2. Gravação das evidências brutas contendo `url_real` verbatim em `docs/mapeamento/evidencias/`.
3. Elaboração das fichas técnicas em `docs/apps/` com dados sustentados estritamente pelas evidências inspecionadas.
4. Sincronização em `config/roteiro.json`.
5. Proposta de classificação em `docs/SAFELIST_LEITURA.md`.
6. Registro append-only dos metadados e hashes em `docs/mapeamento/AUDITORIA.jsonl`.

## O que bloqueou e soluções

- **EGWV005:** Resumo de Faturamento (`https://www.saneago.com.br/prt/egw/EGW005EnvioResumoFaturamento.zul`). Acessada via fallback no menu corporativo (*Grandes Clientes Web -> Cadastro -> Resumo de Faturamento*). Apresentou 1 campo editável (`Conta Macro/Condomínio`) e 2 botões acionáveis (`Consultar`, `Cancelar`). Classificada como `somente_leitura`.
- **EGWV402:** Faturamento (`https://www.saneago.com.br/prt/egw/EGW402GraficoFaturamento.zul`). Acessada via fallback no menu corporativo (*Grandes Clientes Web -> Gráfico -> Faturamento*). Apresentou 7 campos (conta, período, data e checkboxes), 3 botões acionáveis (`Visualizar`, `Imprimir`, `Cancelar`) e 2 botões técnicos sem rótulo declarados em `botoes_ignorados`. Classificada como `somente_leitura`.
- **FGCV018:** Processo (`https://www.saneago.com.br/prt/fgc/FGC018ConsultaProcesso.zul`). Acessada via fallback no menu corporativo (*Gestão Contratos -> Consulta -> Processo*). Apresentou 2 campos (processo) e 3 botões acionáveis (`Consultar`, `Imprimir`, `Cancelar`). Classificada como `somente_leitura`.

## Decisões Tomadas e Classes Propostas

- **EGWV005 (`somente_leitura`):** Resumo de Faturamento. Consulta de faturamento por conta macro/condomínio sem escrita.
- **EGWV402 (`somente_leitura`):** Faturamento. Consulta e visualização de gráficos de faturamento sem escrita.
- **FGCV018 (`somente_leitura`):** Processo. Consulta e impressão de processos de contrato sem escrita.

## Dúvidas para o Gate Humano

- Homologação das classificações propostas para a safelist da Fase 2.

## Verificação e Auditoria

```bash
$ node -e "require('./config/roteiro.json')"
# JSON válido

$ node scripts/auditar-mapeamento.js
Auditadas 363 apps · 357 com entrada em roteiro.json
0 divergências.

$ npm test
# tests 89, pass 89, fail 0
```
