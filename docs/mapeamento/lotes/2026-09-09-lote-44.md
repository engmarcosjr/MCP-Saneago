# Diário de Bordo — Lote 44

**Data:** 2026-09-09  
**Executor:** omniclaude  
**Aplicações:** ECO623, ECO625, ECO635  

---

## 1. O que foi feito

1. **Captura e Inspeção Passiva:**
   - As aplicações `ECO623`, `ECO625` e `ECO635` foram abertas e inspecionadas passivamente pelo script `scripts/capturar_lote.sh` via Playwright.
   - As evidências brutas foram salvas em `docs/mapeamento/evidencias/<CODIGO>.inspect.json` com URLs `.zul` reais e estruturação completa dos componentes de tela.

2. **Documentação e Fichas Técnicas:**
   - Atualizadas as fichas técnicas em `docs/apps/ECO623.md`, `docs/apps/ECO625.md` e `docs/apps/ECO635.md`.
   - Atualizado o catálogo operacional `config/roteiro.json` com os metadados de tela, campos, tipos e botões.

3. **Safelist e Auditoria:**
   - Registradas as propostas de classificação em `docs/SAFELIST_LEITURA.md`.
   - Inseridas as linhas de auditoria append-only em `docs/mapeamento/AUDITORIA.jsonl` contendo os hashes SHA-256 das evidências e fichas, com reconciliação estrita de `botoes_ignorados`.

---

## 2. Resultados e Classificação Proposta

| Código | Nome da Aplicação | Botões Declarados | Botões Ignorados | Classe Proposta | Motivo Principal |
|---|---|---|---|---|---|
| **ECO623** | Demonstrativo Arrecadação Diária | `Imprimir`, `Cancelar` | `Sem Rotulo` (x7) | `somente_leitura` | Emissão e impressão de demonstrativo de arrecadação diária sem botões de escrita. |
| **ECO625** | Declaração de quitação anual de débitos | `Consultar`, `Cancelar` | `Sem Rotulo` (x1) | `somente_leitura` | Consulta e emissão de declaração de quitação anual de débitos por conta sem botões de escrita. |
| **ECO635** | Emitir Certidão Negativa | `Consultar`, `Cancelar` | - | `somente_leitura` | Consulta e emissão de certidão negativa de débitos por conta sem botões de escrita. |

---

## 3. Validação e Provas

- **Sintaxe do Roteiro:** `node -e "require('./config/roteiro.json')"` (OK)
- **Testes Unitários/Offline:** `npm test` (0 falhas, 89 testes aprovados)
- **Script de Auditoria:** `node scripts/auditar-mapeamento.js` (0 divergências, 129 apps auditadas)

