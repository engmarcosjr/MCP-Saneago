# Diário de Bordo — Mapeamento Lote 95

- **Data:** 2026-09-11
- **Lote:** lote-95
- **Executor:** omniclaude
- **Aplicações do Lote:** LQAV083, LQA036, LQA081

---

## O que foi tentado

Inspeção direta e inventário do lote 95:
1. Execução direta da captura automatizada via Playwright para `LQAV083`, `LQA036` e `LQA081`.
2. Gravação das evidências brutas contendo `url_real` verbatim em `docs/mapeamento/evidencias/`.
3. Elaboração das fichas técnicas em `docs/apps/` com dados sustentados estritamente pelas evidências inspecionadas.
4. Sincronização em `config/roteiro.json`.
5. Proposta de classificação em `docs/SAFELIST_LEITURA.md`.
6. Registro append-only dos metadados e hashes em `docs/mapeamento/AUDITORIA.jsonl`.

## O que bloqueou e soluções

- **LQAV083:** Resumo das Irregularidades (`https://www.saneago.com.br/prt/lqa/LQA083ResumoIrregularidades.zul`). Acessada via fallback no menu corporativo (*CONTROLE DE QUALIDADE DA ÁGUA -> Relatório -> Resumo das Irregularidades*). Apresentou 12 campos de filtros, 2 botões acionáveis (`Imprimir`, `Cancelar`) e 6 botões técnicos sem rótulo declarados em `botoes_ignorados`. Classificada como `somente_leitura`.
- **LQA036:** DADOS PARA CONSTRUÇÃO DO RELAT SISÁGUA (`https://www.saneago.com.br/prt/lqa/LQA036RelatorioSISAGUA.zul`). Apresentou 5 campos, grade analítica com 16 colunas, 3 botões acionáveis (`Consultar`, `Ocultar Colunas`, `Cancelar`) e 4 botões técnicos sem rótulo declarados em `botoes_ignorados`. Classificada como `somente_leitura`.
- **LQA081:** Gráfico do IQA Médio por Cidade (`https://www.saneago.com.br/prt/lqa/LQA081GraficoIQAMedio.zul`). Apresentou 17 campos de filtros, 2 botões acionáveis (`Imprimir`, `Cancelar`) e 7 botões técnicos sem rótulo declarados em `botoes_ignorados`. Classificada como `somente_leitura`.

## Decisões Tomadas e Classes Propostas

- **LQAV083 (`somente_leitura`):** Resumo das Irregularidades. Emissão de relatório de resumo das irregularidades sem botões de escrita.
- **LQA036 (`somente_leitura`):** DADOS PARA CONSTRUÇÃO DO RELAT SISÁGUA. Consulta de análises para relatório SISÁGUA sem botões de escrita.
- **LQA081 (`somente_leitura`):** Gráfico do IQA Médio por Cidade. Consulta e emissão de gráfico sem botões de escrita.

## Dúvidas para o Gate Humano

- Homologação das classificações propostas para a safelist da Fase 2.

## Verificação e Auditoria

```bash
$ node -e "require('./config/roteiro.json')"
# JSON válido

$ node scripts/auditar-mapeamento.js
Auditadas 282 apps · 279 com entrada em roteiro.json
0 divergências.

$ npm test
# tests 89, pass 89, fail 0
```
