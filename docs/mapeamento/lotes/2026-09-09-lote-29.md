# Diário de Bordo — Lote 29

- **Data:** 2026-09-09
- **Executor:** omniclaude
- **Aplicações do Lote:** ECO407, ECO410, ECO411

---

## 1. O que foi feito

1. **Inspeção de Telas ZK:**
   - `ECO407` — Cadastramento de Códigos de Lançamentos: tela para consultar códigos de lançamentos cadastrados no sistema. 2 campos de entrada (`Código de Lançamento` editável e readonly) e 2 botões acionáveis (`Consultar`, `Cancelar`), além de botões técnicos auxiliares (`-btn`). Classe proposta: `somente_leitura`.
   - `ECO410` — Cancelamento de Débito/Crédito: tela para consultar lançamentos e cancelamentos de débitos e créditos de contas e documentos financeiros no sistema comercial. 4 campos de entrada (`Número do documento`, `Número de conta/DV` editável e readonly, `Referência`) e 2 botões acionáveis (`Consultar`, `Cancelar`). Grade com 7 colunas (`Número`, `Situação`, `Conta`, `Data Emissão`, `Data Venc.`, `CPF/CNPJ`, `Valor`). Classe proposta: `somente_leitura`.
   - `ECO411` — Abertura de Financiamento: tela para consultar processos de abertura de financiamento por conta/DV ou número do financiamento. 3 campos de entrada (`Conta`, `Conta DV`, `Número do Financiamento`) e 2 botões acionáveis (`Consultar`, `Cancelar`). Classe proposta: `somente_leitura`.

2. **Evidências e Fichas:**
   - Evidências brutas salvas em `docs/mapeamento/evidencias/{ECO407,ECO410,ECO411}.inspect.json`.
   - Fichas técnicas criadas em `docs/apps/{ECO407,ECO410,ECO411}.md`.
   - Catálogo de runtime atualizado em `config/roteiro.json`.
   - Safelist atualizada em `docs/SAFELIST_LEITURA.md`.

3. **Auditoria e Hashes:**
   - Entradas registradas no `docs/mapeamento/AUDITORIA.jsonl` com cálculo estrito de SHA-256 das evidências e fichas e segregação de botões técnicos auxiliares em `botoes_ignorados`.

---

## 2. Prova de Execução

```bash
$ node -e "require('./config/roteiro.json')"
$ node scripts/auditar-mapeamento.js && npm test
Auditadas 84 apps · 81 com entrada em roteiro.json
0 divergências.

> mcp-saneago@1.0.0 test
> node --test test/*.test.js

# tests 89
# suites 0
# pass 89
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

---

## 3. Decisões e Dúvidas para o Gate Humano

- **ECO407, ECO410, ECO411:** Todas as três aplicações foram propostas como `somente_leitura`, pois contêm exclusivamente botões de consulta e cancelamento de operação na tela inicial (`Consultar`, `Cancelar`), sem botões de gravação, persistência ou alteração de estado visíveis no inventário passivo da Fase 1.
