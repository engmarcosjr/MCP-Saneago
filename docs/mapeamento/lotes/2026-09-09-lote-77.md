# Diário de Bordo — Lote 77

**Data:** 2026-09-09  
**Executor:** omniclaude  
**Aplicações:** `PGT912`, `PGT913`, `PGT916`

## 1. Resumo da Rodada
- **PGT912** (`Ordens de Tráfego p/ Período e Situação`): Tela mista de relatório de ordens de tráfego com filtros por período e seleção de situações (`Aberta`, `Reservada`, `Empenhada`, `Encerrada`). Possui botões de ação `Consultar` e `Cancelar`. Classificada como `somente_leitura`.
- **PGT913** (`Relatório de Movimentação de Veículos`): Tela mista de relatório de movimentação com filtros por período, placa e lista de placas em textarea. Possui botões de ação `Imprimir`, `Planilha` e `Cancelar`, além de botões sem rótulo auxiliares. Classificada como `somente_leitura`.
- **PGT916** (`Motoristas`): Tela mista de relatório de motoristas com filtros por U.O, tipo de autorização e formato de saída (`PDF`, `Planilha`). Possui botões de ação `Imprimir` e `Cancelar`. Classificada como `somente_leitura`.

## 2. Decisões e Elementos Ignorados
- **PGT912**: 2 botões de ação (`Consultar`, `Cancelar`) documentados. 2 botões auxiliares do tipo `a` (`r5iId-btn`, `r5iIf-btn` — acionadores de datebox) foram classificados como `botoes_ignorados` (`Sem Rotulo`).
- **PGT913**: 5 botões de ação (`Sem Rotulo`, `Sem Rotulo`, `Imprimir`, `Planilha`, `Cancelar`) documentados. 2 botões auxiliares do tipo `a` (`wNFUn-btn`, `wNFUp-btn` — acionadores de datebox) foram classificados como `botoes_ignorados` (`Sem Rotulo`).
- **PGT916**: 2 botões de ação (`Imprimir`, `Cancelar`) documentados. 1 botão auxiliar do tipo `a` (`aASUh-btn` — acionador/consulta da U.O) foi classificado como `botoes_ignorados` (`Sem Rotulo`).

## 3. Validação e Provas
- `node -e "require('./config/roteiro.json')"`: OK (JSON válido).
- `node scripts/auditar-mapeamento.js`: 0 divergências.
- `npm test`: 0 falhas (89 pass).

