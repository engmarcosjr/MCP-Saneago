# Diário de Bordo — Lote 76

**Data:** 2026-09-09  
**Executor:** omniclaude  
**Aplicações:** `PGT504`, `PGT510`, `PGT511`

## 1. Resumo da Rodada
- **PGT504** (`Registro Offline`): Tela ZK de validação de dados para registro offline de ordens de tráfego com campos para posto de serviço, solicitante, motorista, licença e placa de veículo. Possui 3 botões de validação (`Validar` para solicitante, licença e placa). Classificada como `somente_leitura`.
- **PGT510** (`Atendimento`): Tela mista de consulta e atendimento de ordens de tráfego com filtros por posto de serviço, checkboxes de situação e número da O.T. Possui botão de ação `Atualizar`. Classificada como `somente_leitura`.
- **PGT511** (`Empregados Ordem de Tráfego`): Tela ZK de vinculação de empregados na ordem de tráfego com campos para identificação da O.T e seleção do empregado. Possui botões `Close`, `Incluir` e `Cancelar`. Classificada como `possui_escrita` devido ao botão de inclusão.

## 2. Decisões e Elementos Ignorados
- **PGT504**: 3 botões de ação (`Validar`, `Validar`, `Validar`) documentados. 3 botões auxiliares do tipo `a` (`sTcVe-btn`, `sTcVq-btn`, `sTcV_0-btn` — acionadores de comboboxes e consulta de motorista) foram classificados como `botoes_ignorados` (`Sem Rotulo`).
- **PGT510**: 1 botão de ação (`Atualizar`) documentado. 2 botões auxiliares (`cNBBi-btn` do tipo `a` e `cNBB60-a` do tipo `submit`) foram classificados como `botoes_ignorados` (`Sem Rotulo`).
- **PGT511**: 3 botões de ação (`Close`, `Incluir`, `Cancelar`) documentados. 2 botões auxiliares (`nUmNl-btn` do tipo `a` e `nUmN20-a` do tipo `submit`) foram classificados como `botoes_ignorados` (`Sem Rotulo`).

## 3. Validação e Provas
- `node -e "require('./config/roteiro.json')"`: OK (JSON válido).
- `node scripts/auditar-mapeamento.js`: 0 divergências.
- `npm test`: 0 falhas (89 pass).

