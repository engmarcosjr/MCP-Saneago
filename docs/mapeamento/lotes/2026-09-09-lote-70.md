# Diário de Bordo — Lote 70

**Data:** 2026-09-09  
**Executor:** omniclaude  
**Aplicações:** `LRSV105`, `LRSV130`, `LRSV701`

## 1. Resumo da Rodada
- **LRSV105** (`Lançamento de serviços executados`): Tela ZK de cadastro de retorno de RA e lançamento de serviços executados com botões `Consultar` e `Cancelar`. Como a finalidade da aplicação é cadastro e retorno de serviço com fluxo de persistência, classificada preventivamente como `possui_escrita`.
- **LRSV130** (`Equipes`): Tela mista de consulta e listagem de equipes por distrito, situação e tipo. Apresenta filtros diversos e botões `Consultar` e `Cancelar`. Classificada como `somente_leitura`.
- **LRSV701** (`Relação de Serviços Executados`): Tela mista de consulta e relatório de serviços executados com filtros de UO, período, serviço, prazo, subdelegação e ordenação, com botões `Consultar` e `Cancelar`. Classificada como `somente_leitura`.

## 2. Decisões e Elementos Ignorados
- **LRSV105**: 1 botão auxiliar `Sem Rotulo` (id `p66Ry-btn`, tipo `a`) classificado em `botoes_ignorados` como lookup de Serviço Resposta.
- **LRSV130**: 2 botões auxiliares `Sem Rotulo` (ids `g1DVr-btn`, `g1DVb2-btn`, tipo `a`) classificados em `botoes_ignorados` como lookup de Distrito e dropdown de Tipo de equipe.
- **LRSV701**: 4 botões auxiliares `Sem Rotulo` (ids `r1FNh-btn`, `r1FN40-btn`, `r1FN80-btn`, `r1FNi0-btn`, tipo `a`) classificados em `botoes_ignorados` como lookups e calendários.

## 3. Validação e Provas
- `node -e "require('./config/roteiro.json')"`: OK (JSON válido).
- `node scripts/auditar-mapeamento.js`: 0 divergências (207 apps auditadas · 204 com entrada em roteiro.json).
- `npm test`: 83 testes / 89 subtestes com 0 falhas.
