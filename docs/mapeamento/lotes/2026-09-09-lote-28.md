# Diário de Bordo — Lote 28

- **Data:** 2026-09-09
- **Executor:** omniclaude
- **Aplicações do Lote:** ECO395, ECO402, ECO404

---

## 1. O que foi feito

1. **Inspeção de Telas ZK:**
   - `ECO395` — Clientes com Irregularidades: tela para consultar e emitir relatórios de clientes com irregularidades, filtrando por localização, período e situação (Aberto / Pendente) em formato PDF ou Planilha. 22 campos de entrada, 2 botões acionáveis (`Imprimir`, `Cancelar`) e 8 botões técnicos auxiliares (`-btn`). Classe proposta: `somente_leitura`.
   - `ECO402` — Cadastro de Braille / Contas Braille: tela para consultar cadastros de clientes com solicitação de faturas em Braille e emitir relação de contas ativas. 1 campo de entrada (`Número de Conta/DV`) e 3 botões acionáveis (`Consultar`, `Imprimir Contas Ativas`, `Cancelar`). Classe proposta: `somente_leitura`.
   - `ECO404` — Cadastramento Das Tarifas: tela para consultar as tabelas tarifárias de água, esgoto e serviços cadastradas no sistema. 1 campo de entrada (`N° Tabela`) e 2 botões acionáveis (`Consultar`, `Cancelar`). Classe proposta: `somente_leitura`.

2. **Evidências e Fichas:**
   - Evidências brutas salvas em `docs/mapeamento/evidencias/{ECO395,ECO402,ECO404}.inspect.json`.
   - Fichas técnicas criadas em `docs/apps/{ECO395,ECO402,ECO404}.md`.
   - Catálogo de runtime atualizado em `config/roteiro.json`.
   - Safelist atualizada em `docs/SAFELIST_LEITURA.md`.

3. **Auditoria e Hashes:**
   - Entradas registradas no `docs/mapeamento/AUDITORIA.jsonl` com cálculo estrito de SHA-256 das evidências e fichas e segregação de botões técnicos auxiliares em `botoes_ignorados`.

---

## 2. Prova de Execução

```bash
$ node -e "require('./config/roteiro.json')"
$ node scripts/auditar-mapeamento.js && npm test
Auditadas 81 apps · 78 com entrada em roteiro.json
0 divergências.

> mcp-saneago@1.0.0 test
> node --test test/*.test.js

# tests 89
# suites 0
# pass 89
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

---

## 3. Decisões e Dúvidas para o Gate Humano

- **ECO395, ECO402, ECO404:** Todas as três aplicações foram propostas como `somente_leitura`, pois contêm exclusivamente botões de consulta, emissão e exportação (`Consultar`, `Imprimir`, `Imprimir Contas Ativas`, `Cancelar`), sem botões de gravação, alteração de estado ou submissão cadastral.
