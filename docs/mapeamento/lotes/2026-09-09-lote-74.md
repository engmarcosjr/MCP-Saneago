# Diário de Bordo — Lote 74

**Data:** 2026-09-09  
**Executor:** omniclaude  
**Aplicações:** `PGTV912`, `PGTV916`, `PGTV918`

## 1. Resumo da Rodada
- **PGTV912** (`Ordens de Tráfego p/ Período e Situação`): Tela mista de consulta e relatório de Ordens de Tráfego por período e checkboxes de situação (`Aberta`, `Reservada`, `Empenhada`, `Encerrada`). Possui botões de ação `Consultar` e `Cancelar`. Classificada como `somente_leitura`.
- **PGTV916** (`Motoristas`): Tela mista de relatório de motoristas e autorizações com filtros por UO, tipo de autorização e formato de saída (PDF/Planilha). Possui botões de ação `Imprimir` e `Cancelar`. Classificada como `somente_leitura`.
- **PGTV918** (`Ordem de tráfego por motorista`): Tela ZK de relatório de ordens de tráfego por motorista com filtros por matrícula, período e combobox de situação. Possui botões de ação `Emitir` e `Cancelar`. Classificada como `somente_leitura`.

## 2. Decisões e Elementos Ignorados
- **PGTV912**: 2 botões de ação (`Consultar`, `Cancelar`) documentados. 2 botões auxiliares do tipo `a` (`hS8Ud-btn`, `hS8Uf-btn` — acionadores de datebox) foram classificados como `botoes_ignorados` (`Sem Rotulo`).
- **PGTV916**: 2 botões de ação (`Imprimir`, `Cancelar`) documentados. 1 botão auxiliar do tipo `a` (`kCsWh-btn` — acionador/consulta da U.O) foi classificado como `botoes_ignorados` (`Sem Rotulo`).
- **PGTV918**: 2 botões de ação (`Emitir`, `Cancelar`) documentados. 4 botões auxiliares do tipo `a` (`wDJJg-btn`, `wDJJ_0-btn`, `wDJJ10-btn`, `wDJJ60-btn` — acionadores de busca de matrícula, dateboxes e combobox) foram classificados como `botoes_ignorados` (`Sem Rotulo`).

## 3. Validação e Provas
- `node -e "require('./config/roteiro.json')"`: OK (JSON válido).
- `node scripts/auditar-mapeamento.js`: 0 divergências.
- `npm test`: 0 falhas.
