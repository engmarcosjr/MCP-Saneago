# Diário de Bordo — Lote 21

- **Data:** 2026-09-09
- **Executor:** omniclaude
- **Aplicações do Lote:** ECO204, ECO205, ECO212

---

## 1. O que foi feito

1. **Inspeção de Telas ZK:**
   - `ECO204` — Ocorrências de Leitura/Corte/Relig: tela para consultar, filtrar e incluir ocorrências. 17 campos de entrada (rádios, textos e comboboxes) e 4 botões acionáveis (`Consultar`, `Incluir`, `Imprimir`, `Cancelar`) + 6 botões técnicos do ZK (5 seletores e 1 submit sem rótulo). Grade com colunas Código, Descrição, Necessidade, Permissão, Foto, Contabilização, Situação, Retenção, Ações. Classe proposta: `possui_escrita` devido ao botão de ação `Incluir`.
   - `ECO205` — Hidrômetros: tela de consulta de informações e histórico de hidrômetros por conta ou por número. 5 campos de entrada (rádios e textos) e 2 botões acionáveis (`Consultar`, `Cancelar`). Classe proposta: `somente_leitura`.
   - `ECO212` — Consumo Hidrômetro Instal/Trocado: tela para consultar e emitir relatórios/planilhas de consumo de hidrômetros instalados ou trocados por cidade, bairro, período de movimentação e características. 25 campos de entrada e 4 botões acionáveis (`Consultar`, `Imprimir`, `Planilha`, `Cancelar`) + 4 botões técnicos de combobox. Grade com colunas Referência, Consumo, Valor, Contas Faturadas. Classe proposta: `somente_leitura`.

2. **Evidências e Fichas:**
   - Evidências brutas salvas em `docs/mapeamento/evidencias/{ECO204,ECO205,ECO212}.inspect.json`.
   - Fichas técnicas criadas em `docs/apps/{ECO204,ECO205,ECO212}.md`.
   - Catálogo de runtime atualizado em `config/roteiro.json`.
   - Safelist atualizada em `docs/SAFELIST_LEITURA.md`.

3. **Auditoria e Hashes:**
   - Entradas registradas no `docs/mapeamento/AUDITORIA.jsonl` com cálculo estrito de SHA-256 das evidências e fichas e segregação de botões técnicos auxiliares em `botoes_ignorados`.

---

## 2. Prova de Execução

```bash
$ node scripts/auditar-mapeamento.js && npm test
Auditadas 60 apps · 57 com entrada em roteiro.json
0 divergências.

> mcp-saneago@1.0.0 test
> node --test test/*.test.js

# tests 89
# suites 0
# pass 89
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

---

## 3. Decisões e Dúvidas para o Gate Humano

- **ECO204:** Proposta como `possui_escrita` devido à presença explícita do botão de ação `Incluir`.
- **ECO205, ECO212:** Propostas como `somente_leitura`, pois contêm apenas botões de consulta, emissão e exportação (`Planilha`) sem alteração ou gravação de dados.
