# Diário de Bordo — Mapeamento Lote 91

- **Data:** 2026-09-11
- **Lote:** lote-91
- **Executor:** omniclaude
- **Aplicações do Lote:** LQE030, LQE039, LQEV025

---

## O que foi tentado

Inspeção direta e inventário do lote 91:
1. Execução direta da captura automatizada via Playwright para `LQE030`, `LQE039` e `LQEV025`.
2. Gravação das evidências brutas contendo `url_real` verbatim em `docs/mapeamento/evidencias/`.
3. Elaboração das fichas técnicas em `docs/apps/` com dados sustentados estritamente pelas evidências inspecionadas.
4. Sincronização em `config/roteiro.json`.
5. Proposta de classificação em `docs/SAFELIST_LEITURA.md`.
6. Registro append-only dos metadados e hashes em `docs/mapeamento/AUDITORIA.jsonl`.

## O que bloqueou e soluções

- **LQE030:** Resultados das Análises - Planilha (`https://www.saneago.com.br/prt/lqe/LQE030ResultadoAnalisePlanilha.zul`). Apresentou 156 campos (sistema, período de coleta e checkboxes para parâmetros físico-químicos e biológicos), 3 botões acionáveis (`Consultar`, `Imprimir`, `Cancelar`) e 4 botões técnicos sem rótulo declarados em `botoes_ignorados`. Classificada como `somente_leitura`.
- **LQE039:** Relatório do IQEt por data (`https://www.saneago.com.br/prt/lqe/LQE039RelatorioIQEt.zul`). Apresentou 10 campos de filtro geográfico e temporal, 2 botões acionáveis (`Imprimir`, `Cancelar`) e 6 botões técnicos sem rótulo declarados em `botoes_ignorados`. Classificada como `somente_leitura`.
- **LQEV025:** Consultar Amostras (`https://www.saneago.com.br/prt/lqe/LQE025ConsultaAmostra.zul`). Acessada via fallback no menu corporativo (*Indice Qualidade Esgoto -> Consulta -> Consultar Amostras*). Apresentou 7 campos de filtros, grade com 7 colunas, 2 botões acionáveis (`Consultar`, `Cancelar`) e 6 botões técnicos sem rótulo declarados em `botoes_ignorados`. Classificada como `somente_leitura`.

## Decisões Tomadas e Classes Propostas

- **LQE030 (`somente_leitura`):** Resultados das Análises - Planilha. Consulta e exportação em planilha de análises laboratoriais sem botões de escrita.
- **LQE039 (`somente_leitura`):** Relatório do IQEt por data. Emissão de relatório do IQEt por período sem botões de escrita.
- **LQEV025 (`somente_leitura`):** Consultar Amostras. Consulta de amostras laboratoriais sem botões de escrita.

## Dúvidas para o Gate Humano

- Homologação das classificações propostas para a safelist da Fase 2.

## Verificação e Auditoria

```bash
$ node -e "require('./config/roteiro.json')"
# JSON válido

$ node scripts/auditar-mapeamento.js
Auditadas 270 apps · 267 com entrada em roteiro.json
0 divergências.

$ npm test
# tests 89, pass 89, fail 0
```
