# Diário de Bordo — Lote 27

- **Data:** 2026-09-09
- **Executor:** omniclaude
- **Aplicações do Lote:** ECO367, ECO376, ECO385

---

## 1. O que foi feito

1. **Inspeção de Telas ZK:**
   - `ECO367` — Clientes c/ Média de Consumo X: tela para consultar e emitir relatórios de clientes com média de consumo acima ou abaixo de determinado valor de referência por período e localização. 24 campos de entrada (textos, datas e rádios), 2 botões acionáveis (`Imprimir`, `Cancelar`) e 6 botões técnicos auxiliares (`-btn`). Classe proposta: `somente_leitura`.
   - `ECO376` — Clientes c/ Consumo Faturado: tela para consultar e emitir relatórios de clientes com consumo faturado estimado acima ou abaixo de determinado valor de referência em PDF ou Planilha. 24 campos de entrada (textos, datas e rádios), 2 botões acionáveis (`Imprimir`, `Cancelar`) e 6 botões técnicos auxiliares (`-btn`). Classe proposta: `somente_leitura`.
   - `ECO385` — Segunda Crítica de Consumo (Anormalidades não criticadas na ECO359): tela para consultar e gerenciar anormalidades de leitura e consumo não criticadas na ECO359, permitindo disparar a execução de job de processamento no servidor. 17 campos de entrada, 3 botões acionáveis (`Consultar`, `Executar Job`, `Cancelar`) e 5 botões técnicos auxiliares (`-btn`). Grade de dados com 11 colunas. Classe proposta: `possui_escrita`.

2. **Evidências e Fichas:**
   - Evidências brutas salvas em `docs/mapeamento/evidencias/{ECO367,ECO376,ECO385}.inspect.json`.
   - Fichas técnicas criadas em `docs/apps/{ECO367,ECO376,ECO385}.md`.
   - Catálogo de runtime atualizado em `config/roteiro.json`.
   - Safelist atualizada em `docs/SAFELIST_LEITURA.md`.

3. **Auditoria e Hashes:**
   - Entradas registradas no `docs/mapeamento/AUDITORIA.jsonl` com cálculo estrito de SHA-256 das evidências e fichas e segregação de botões técnicos auxiliares em `botoes_ignorados`.

---

## 2. Prova de Execução

```bash
$ node scripts/auditar-mapeamento.js && npm test
Auditadas 78 apps · 75 com entrada em roteiro.json
0 divergências.

> mcp-saneago@1.0.0 test
> node --test test/*.test.js

# tests 89
# suites 0
# pass 89
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

---

## 3. Decisões e Dúvidas para o Gate Humano

- **ECO367, ECO376:** Propostas como `somente_leitura`, pois contêm exclusivamente botões de emissão de relatório impresso e planilha (`Imprimir`, `Cancelar`) sem gravação de dados.
- **ECO385:** Proposta como `possui_escrita` devido à presença do botão `Executar Job`, requerendo supervisão e gate de confirmação estrito.
