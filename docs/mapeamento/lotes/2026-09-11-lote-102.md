# Diário de Bordo — Mapeamento Lote 102

- **Data:** 2026-09-11
- **Lote:** lote-102
- **Executor:** omniclaude
- **Aplicações do Lote:** MSS502, MSS503, MSS550

---

## O que foi tentado

Inspeção direta e inventário do lote 102:
1. Execução direta da captura automatizada via Playwright para `MSS502`, `MSS503` e `MSS550`.
2. Gravação das evidências brutas contendo `url_real` verbatim em `docs/mapeamento/evidencias/`.
3. Elaboração das fichas técnicas em `docs/apps/` com dados sustentados estritamente pelas evidências inspecionadas.
4. Sincronização em `config/roteiro.json`.
5. Proposta de classificação em `docs/SAFELIST_LEITURA.md`.
6. Registro append-only dos metadados e hashes em `docs/mapeamento/AUDITORIA.jsonl`.

## O que bloqueou e soluções

- **MSS502:** Sistemas e Aplicações Cobol - Legado (`https://www.saneago.com.br/prt/mss/MSS502ConsultaFuncaoCobol.zul`). Apresentou 1 campo editável (`Nome`), 3 botões acionáveis (`Consultar`, `Imprimir`, `Cancelar`) e 3 botões técnicos de grade sem rótulo declarados em `botoes_ignorados`. Classificada como `somente_leitura`.
- **MSS503:** Usuário por Aplicação - Legado BZ127 (`https://www.saneago.com.br/prt/mss/MSS503ConsultaAcessoCobol.zul`). Apresentou 2 campos (função), 2 botões acionáveis (`Consultar`, `Cancelar`) e 1 botão técnico de combobox sem rótulo declarado em `botoes_ignorados`. Classificada como `somente_leitura`.
- **MSS550:** Consulta A. Complementar por Aplicação (`https://www.saneago.com.br/prt/mss/MSS550ConsultaAcessoComplementar.zul`). Apresentou 2 campos (sistema), 2 botões acionáveis (`Consultar`, `Cancelar`) e 1 botão técnico de combobox sem rótulo declarado em `botoes_ignorados`. Classificada como `somente_leitura`.

## Decisões Tomadas e Classes Propostas

- **MSS502 (`somente_leitura`):** Sistemas e Aplicações Cobol - Legado. Consulta e emissão de relatório de funções Cobol legadas sem escrita.
- **MSS503 (`somente_leitura`):** Usuário por Aplicação - Legado BZ127. Consulta de usuários autorizados por função Cobol sem escrita.
- **MSS550 (`somente_leitura`):** Consulta A. Complementar por Aplicação. Consulta de permissões complementares sem escrita.

## Dúvidas para o Gate Humano

- Homologação das classificações propostas para a safelist da Fase 2.

## Verificação e Auditoria

```bash
$ node -e "require('./config/roteiro.json')"
# JSON válido

$ node scripts/auditar-mapeamento.js
Auditadas 303 apps · 300 com entrada em roteiro.json
0 divergências.

$ npm test
# tests 89, pass 89, fail 0
```
