# Diário de Mapeamento — Lote 31

**Data:** 2026-09-09  
**Executor:** omniclaude  
**Lote:** 31  
**Aplicações:** ECO422, ECO427, ECO460

---

## 1. Visão Geral do Lote

| Código | Nome | URL ZUL Real | Campos | Botões | Classe Proposta | Motivo / Observações |
|---|---|---|---|---|---|---|
| **ECO422** | Contas Pagas com Atraso | `https://www.saneago.com.br/prt/eco/ECO422ContaPagaAtraso.zul` | 2 | 2 (`Consultar`, `Cancelar`) | `somente_leitura` | Consulta de contas pagas em atraso por conta e mês/ano de referência com colunas detalhadas de juros/encargos. |
| **ECO427** | Termo de Ocorrência de Irregularidade | `https://www.saneago.com.br/prt/eco/ECO427conIrreg.zul` | 7 | 2 (`Imprimir`, `Cancelar`) | `somente_leitura` | Consulta e emissão/impressão de termo de ocorrência de irregularidade por RA e parâmetros. |
| **ECO460** | Emite Faturamento Avulso | `https://www.saneago.com.br/prt/eco/ECO460FaturamentoAvulso.zul` | 10 | 2 (`Consultar`, `Cancelar`) | `somente_leitura` | Consulta e emissão de faturamento avulso por conta/documento/CPF/CNPJ e situação. |

---

## 2. Decisões e Tratamento de Controles Técnicos

1. **ECO422**:
   - Tela ZK limpa com 2 filtros principais (`Número Conta/DV` e `Mês/Ano Referência`).
   - Botão auxiliar `rEwZl-btn` (abridor de calendário) identificado com `label: "Sem Rotulo"` e registrado em `botoes_ignorados`.
   - Grid com 12 colunas detalhadas de histórico de faturas e encargos.

2. **ECO427**:
   - Tela mista contendo opções de filtro por `RA` ou `Parâmetros`, consulta por número de RA e opção de emissão de BCC.
   - Botão auxiliar `pYxOr5-btn` associado ao combobox de tipo de termo registrado em `botoes_ignorados`.
   - Apenas botões de impressão e cancelamento; proposta de `somente_leitura`.

3. **ECO460**:
   - Tela mista para consulta de faturamento avulso por conta, documento ou CPF/CNPJ com filtros de situação (`Abertos`, `Pagos`, `Todos`).
   - Botão auxiliar `r5nJu0-btn` associado à busca/abertura registrado em `botoes_ignorados`.
   - Botões restritos a `Consultar` e `Cancelar`; sem persistência de estado.

---

## 3. Validação e Auditoria

```bash
$ node -e "require('./config/roteiro.json')"
OK

$ node scripts/auditar-mapeamento.js
Auditoria concluída com 0 divergências.

$ npm test
✔ todos os testes passaram (89 testes)
```
