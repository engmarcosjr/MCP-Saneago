# Diário de Bordo — Mapeamento Lote 94

- **Data:** 2026-09-11
- **Lote:** lote-94
- **Executor:** omniclaude
- **Aplicações do Lote:** LQAV028, LQAV071, LQAV081

---

## O que foi tentado

Inspeção direta e inventário do lote 94:
1. Execução direta da captura automatizada via Playwright para `LQAV028`, `LQAV071` e `LQAV081`.
2. Gravação das evidências brutas contendo `url_real` verbatim em `docs/mapeamento/evidencias/`.
3. Elaboração das fichas técnicas em `docs/apps/` com dados sustentados estritamente pelas evidências inspecionadas.
4. Sincronização em `config/roteiro.json`.
5. Proposta de classificação em `docs/SAFELIST_LEITURA.md`.
6. Registro append-only dos metadados e hashes em `docs/mapeamento/AUDITORIA.jsonl`.

## O que bloqueou e soluções

- **LQAV028:** Cadastro de Mananciais/Bacias (`https://www.saneago.com.br/prt/lqa/LQA028cadMananc.jsp`). Acessada via fallback no menu corporativo (*CONTROLE DE QUALIDADE DA ÁGUA -> Cadastro -> Cadastro de Mananciais/Bacias*). Apresentou 2 campos textuais e nenhum botão de ação na tela inicial. Classificada como `somente_leitura`.
- **LQAV071:** Demonstrativo Qtdes de Contas por Bairro (`https://www.saneago.com.br/prt/lqa/LQA071qtdeContas.jsp`). Acessada via fallback no menu corporativo (*CONTROLE DE QUALIDADE DA ÁGUA -> Relatório -> Demonstrativo Qtdes de Contas por Bairro*). Apresentou 4 campos e nenhum botão de ação na tela inicial. Classificada como `somente_leitura`.
- **LQAV081:** Gráfico do IQA Médio por Cidade (`https://www.saneago.com.br/prt/lqa/LQA081GraficoIQAMedio.zul`). Acessada via fallback no menu (*CONTROLE DE QUALIDADE DA ÁGUA -> Relatório -> Gráfico do IQA Médio por Cidade*). Apresentou 17 campos de filtros, 2 botões acionáveis (`Imprimir`, `Cancelar`) e 7 botões técnicos sem rótulo declarados em `botoes_ignorados`. Classificada como `somente_leitura`.

## Decisões Tomadas e Classes Propostas

- **LQAV028 (`somente_leitura`):** Cadastro de Mananciais/Bacias. Consulta cadastral sem botões de escrita.
- **LQAV071 (`somente_leitura`):** Demonstrativo Qtdes de Contas por Bairro. Consulta de demonstrativo sem botões de escrita.
- **LQAV081 (`somente_leitura`):** Gráfico do IQA Médio por Cidade. Consulta e emissão de gráfico sem botões de escrita.

## Dúvidas para o Gate Humano

- Homologação das classificações propostas para a safelist da Fase 2.

## Verificação e Auditoria

```bash
$ node -e "require('./config/roteiro.json')"
# JSON válido

$ node scripts/auditar-mapeamento.js
Auditadas 279 apps · 276 com entrada em roteiro.json
0 divergências.

$ npm test
# tests 89, pass 89, fail 0
```
