# Diário do Lote 60 — Mapeamento Passivo

**Data:** 2026-09-09  
**Executor:** omniclaude  
**Aplicações:** LRSV734, LRSV735, LRS002  

---

## 1. Contexto & Objetivos

Mapeamento passivo (Fase 1 - inventário de tela sem clique nem escrita) do Lote 60 da vertical de Atendimento ao Público / Relatórios (`LRS`).
- **LRSV734**: Atendimento Por Código de Serviço (JSP).
- **LRSV735**: RA-RI por Cidade e Bairro (JSP).
- **LRS002**: RA Pendente (ZK).

---

## 2. Inspeção & Evidências

1. **LRSV734** (`docs/mapeamento/evidencias/LRSV734.inspect.json`)
   - URL Real: `https://www.saneago.com.br/prt/lrs/LRS734relAtendCod.jsp`
   - Tecnologia: HTML / JSP legado
   - Campos (4): `dataInicio` (text, editável), `dataFim` (text, editável), `codigoServico` (text, editável), `descricaoServico` (text, readonly).
   - Botões: nenhum retornado na inspeção inicial.
   - Classe proposta: `somente_leitura`.

2. **LRSV735** (`docs/mapeamento/evidencias/LRSV735.inspect.json`)
   - URL Real: `https://www.saneago.com.br/prt/lrs/LRS735relRACidBai.jsp`
   - Tecnologia: HTML / JSP legado
   - Campos (7): `dataInicio` (text, editável), `dataFim` (text, editável), `codigoCidade` (text, editável), `nomeCidade` (text, readonly), `codigoServico` (text, editável), `descricaoServico` (text, readonly), `tipo` (text, readonly).
   - Botões: nenhum retornado na inspeção inicial.
   - Classe proposta: `somente_leitura`.

3. **LRS002** (`docs/mapeamento/evidencias/LRS002.inspect.json`)
   - URL Real: `https://www.saneago.com.br/prt/lrs/LRS002ConsultaRaPendente.zul`
   - Tecnologia: ZK Framework
   - Campos (4): `o8vIg` (Distrito, text, editável), `o8vIi-real` (Distrito, text, readonly), `o8vI10-real` (Período inicial, date, editável), `o8vI30-real` (Período final, date, editável).
   - Botões (2 ação + 3 ignorados): `Consultar`, `Cancelar`. Ignorados: 3 combobox/lookup `Sem Rotulo`.
   - Colunas: `N° RA`, `Conta`, `Serviço`, `Data Solicit.`, `Data Ult. Rep.`, `Regis. Atend.`, `Cad. Usu.`.
   - Classe proposta: `somente_leitura`.

---

## 3. Validação e Testes

- `node -e "require('./config/roteiro.json')"`: JSON válido.
- `npm test`: 89 testes passando (0 falhas).
- `node scripts/auditar-mapeamento.js`: 0 divergências encontradas.
